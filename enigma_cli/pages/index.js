import React from 'react';
import DatasetTab from './DatasetTab';  // Adjust the path based on your project structure

const Home = () => {
    return (
        <div>
            {/* Other components or content */}
            <DatasetTab />
            {/* Other components or content */}
        </div>
    );
};

export default Home;
